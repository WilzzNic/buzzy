<form method="post" action="{{ path('app_group_channels_delete', {'groupChannelId': group_channel.groupChannelId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ group_channel.groupChannelId) }}">
    <button class="btn">Delete</button>
</form>
